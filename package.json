{
  "name": "accommodation-root",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd .. && python backend/manage.py collectstatic --noinput"
  },
  "engines": {
    "node": "20.x"
  }
}